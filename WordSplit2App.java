package loop;

public class WordSplit2App {
    public static void main(String[] args) {

        String word = "Test";

        for (int i = 0; i < word.length(); i++){
            System.out.println(word.charAt(i));
        }
        // for this word.charAt() better is normal "for loop"
        // first commit
	// next change for git exercise (17)
    }
}
